<!DOCTYPE html>
<html>
  <head>
    <title>问卷调查</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
  <% if(result) { %>
    <h1><%= result.name %></h1>
    <% result.questions.forEach(function(question) { %>
        <% include question %>
    <% }) %>
  <% } %>
  </body>
</html>